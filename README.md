Sources for the plugin `plugin.googleAnalytics.v2`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.googleAnalytics.v2" = {
            publisherId = "com.solar2d",
        },
    },
}
```
